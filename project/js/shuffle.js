import '../style.css';
import { DOMselectors } from './doms';
import { characters } from './endor';

function shuffle(x) {
    return number = Math.floor(Math.random()* x);
};

export {shuffle};
export {number};